@extends('layouts.app')
@section('title', 'Traveler Reviews | UAE Tourism')
@section('content')
<section class="bg-deepblack pb-14 pt-36 text-white"><div class="container mx-auto px-6"><p class="text-sm font-semibold uppercase tracking-[.2em] text-gold">Real stories</p><h1 class="mt-3 text-4xl font-bold">Traveler reviews</h1></div></section><section class="py-14"><div class="container mx-auto px-6"><div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">@forelse($testimonials as $testimonial)<blockquote class="rounded-2xl bg-white p-7 shadow"><p class="text-gold">{{ str_repeat('★', $testimonial->rating) }}</p><p class="mt-4 text-gray-600">“{{ $testimonial->content }}”</p><footer class="mt-5 font-bold">{{ $testimonial->name }} <span class="font-normal text-gray-500">{{ $testimonial->country ? '· '.$testimonial->country : '' }}</span></footer></blockquote>@empty <p class="col-span-full py-16 text-center text-gray-500">No reviews have been published yet.</p>@endforelse</div><div class="mt-8">{{ $testimonials->links() }}</div>@auth <form method="POST" action="{{ route('testimonials.store') }}" class="mx-auto mt-12 max-w-2xl rounded-2xl bg-white p-7 shadow">@csrf<h2 class="text-2xl font-bold">Share your experience</h2><div class="mt-4"><select name="rating" class="rounded-lg border-gray-300">@for($i=5;$i>=1;$i--)<option value="{{ $i }}">{{ $i }} stars</option>@endfor</select></div><textarea required name="content" rows="4" class="mt-4 w-full rounded-lg border-gray-300" placeholder="Tell fellow travelers about your visit"></textarea><button class="mt-4 rounded-lg bg-deepblack px-5 py-3 font-bold text-white">Submit review</button></form>@endauth</div></section>
@endsection
