@extends('layouts.app')
@section('title', 'Contact Us | UAE Tourism')
@section('content')
<section class="pt-32"><div class="container mx-auto max-w-2xl px-6 py-12"><p class="font-semibold uppercase tracking-[.2em] text-gold">We are here to help</p><h1 class="mt-3 text-4xl font-bold">Contact us</h1><p class="mt-3 text-gray-600">Tell us how we can make your UAE journey unforgettable.</p>@if(session('success'))<div class="mt-6 rounded-lg bg-green-50 p-4 text-green-700">{{ session('success') }}</div>@endif<form method="POST" action="{{ route('contact.store') }}" class="mt-8 grid gap-5 rounded-2xl bg-white p-7 shadow">@csrf<input name="name" required value="{{ old('name') }}" class="rounded-lg border-gray-300" placeholder="Your name"><input name="email" required type="email" value="{{ old('email') }}" class="rounded-lg border-gray-300" placeholder="Email address"><input name="phone" value="{{ old('phone') }}" class="rounded-lg border-gray-300" placeholder="Phone number (optional)"><input name="subject" value="{{ old('subject') }}" class="rounded-lg border-gray-300" placeholder="Subject"><textarea name="message" required rows="6" class="rounded-lg border-gray-300" placeholder="How can we help?"></textarea><button class="rounded-lg bg-deepblack py-3 font-bold text-white hover:bg-gold hover:text-deepblack">Send message</button></form></div></section>
@endsection
