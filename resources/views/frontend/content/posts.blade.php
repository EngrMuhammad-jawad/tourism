@extends('layouts.app')
@section('title', 'Travel Journal | UAE Tourism')
@section('content')
<section class="bg-deepblack pb-14 pt-36 text-white"><div class="container mx-auto px-6"><p class="text-sm font-semibold uppercase tracking-[.2em] text-gold">Travel journal</p><h1 class="mt-3 text-4xl font-bold">Stories from the UAE</h1></div></section><section class="py-12"><div class="container mx-auto px-6"><form class="mb-8"><input name="q" value="{{ request('q') }}" class="w-full max-w-md rounded-lg border-gray-300" placeholder="Search stories"><button class="ms-2 rounded-lg bg-deepblack px-4 py-2 text-white">Search</button></form><div class="grid gap-7 md:grid-cols-2 lg:grid-cols-3">@forelse($posts as $post)<article class="overflow-hidden rounded-2xl bg-white shadow"><img class="h-52 w-full object-cover" src="{{ $post->getFirstMediaUrl('featured') ?: asset('assets/images/hero.jpg') }}" alt="{{ $post->title }}"><div class="p-6"><p class="text-sm text-gold">{{ $post->category->name }} · {{ $post->published_at->format('d M Y') }}</p><h2 class="mt-2 text-xl font-bold">{{ $post->title }}</h2><p class="mt-3 line-clamp-3 text-gray-600">{{ $post->excerpt }}</p><a class="mt-5 inline-block font-semibold hover:text-gold" href="{{ route('posts.show', $post) }}">Read story →</a></div></article>@empty <p class="col-span-full py-16 text-center text-gray-500">No stories have been published yet.</p>@endforelse</div><div class="mt-8">{{ $posts->links() }}</div></div></section>
@endsection
