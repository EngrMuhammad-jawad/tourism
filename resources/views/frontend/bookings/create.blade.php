@extends('layouts.app')
@section('title', 'Book '.$bookable->name.' | UAE Tourism')
@section('content')
<section class="pt-32"><div class="container mx-auto max-w-2xl px-6 py-12"><h1 class="text-4xl font-bold">Complete your booking</h1><p class="mt-2 text-gray-600">{{ $bookable->name }}</p><form method="POST" action="{{ route('bookings.store') }}" class="mt-8 space-y-5 rounded-2xl bg-white p-7 shadow-lg">@csrf @php($type = $bookable instanceof \App\Models\TourPackage ? 'package' : ($bookable instanceof \App\Models\Room ? 'room' : 'transport'))<input type="hidden" name="type" value="{{ $type }}"><input type="hidden" name="id" value="{{ $bookable->id }}"><div><label class="block text-sm font-semibold">Travel date</label><input type="date" name="travel_date" min="{{ now()->toDateString() }}" value="{{ old('travel_date') }}" class="mt-1 w-full rounded-lg border-gray-300" required></div><div class="grid gap-5 sm:grid-cols-2"><div><label class="block text-sm font-semibold">Adults</label><input type="number" name="adults" min="1" value="{{ old('adults', 1) }}" class="mt-1 w-full rounded-lg border-gray-300" required></div><div><label class="block text-sm font-semibold">Children</label><input type="number" name="children" min="0" value="{{ old('children', 0) }}" class="mt-1 w-full rounded-lg border-gray-300"></div></div><div><label class="block text-sm font-semibold">Payment preference</label><select name="payment_method" class="mt-1 w-full rounded-lg border-gray-300"><option value="pay_later">Pay later</option><option value="online">Online payment</option></select></div><div><label class="block text-sm font-semibold">Notes (optional)</label><textarea name="customer_note" rows="3" class="mt-1 w-full rounded-lg border-gray-300">{{ old('customer_note') }}</textarea></div>@if($errors->any())<div class="rounded-lg bg-red-50 p-4 text-sm text-red-700">{{ $errors->first() }}</div>@endif<button class="w-full rounded-lg bg-deepblack py-3 font-bold text-white hover:bg-gold hover:text-deepblack">Submit booking request</button></form></div></section>
@endsection
