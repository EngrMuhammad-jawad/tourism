@extends('layouts.app')
@section('title', 'Hotels | UAE Tourism')
@section('content')
<section class="bg-deepblack pb-14 pt-36 text-white"><div class="container mx-auto px-6"><p class="text-sm font-semibold uppercase tracking-[.2em] text-gold">Stay in comfort</p><h1 class="mt-3 text-4xl font-bold md:text-5xl">Hotels</h1></div></section>
<section class="py-12"><div class="container mx-auto px-6"><form class="mb-10 grid gap-3 rounded-2xl bg-white p-5 shadow sm:grid-cols-3"><select name="destination" class="rounded-lg border-gray-200"><option value="">All destinations</option>@foreach($destinations as $destination)<option value="{{ $destination->id }}" @selected(request('destination') == $destination->id)>{{ $destination->name }}</option>@endforeach</select><select name="rating" class="rounded-lg border-gray-200"><option value="">Any rating</option>@for($rating = 5; $rating >= 1; $rating--)<option value="{{ $rating }}" @selected(request('rating') == $rating)>{{ $rating }} stars and up</option>@endfor</select><button class="rounded-lg bg-deepblack px-5 py-2 font-semibold text-white hover:bg-gold hover:text-deepblack">Filter hotels</button></form><div class="grid gap-7 sm:grid-cols-2 lg:grid-cols-3">@forelse($hotels as $hotel)<article class="overflow-hidden rounded-2xl bg-white shadow-lg"><img class="h-56 w-full object-cover" src="{{ $hotel->getFirstMediaUrl('featured') ?: asset('assets/images/hero.jpg') }}" alt="{{ $hotel->name }}"><div class="p-6"><p class="text-gold">{{ str_repeat('★', $hotel->star_rating) }} <span class="text-gray-500">{{ $hotel->destination?->name ?? 'UAE' }}</span></p><h2 class="mt-2 text-xl font-bold">{{ $hotel->name }}</h2><p class="mt-2 line-clamp-2 text-sm text-gray-600">{{ $hotel->description }}</p><a href="{{ route('hotels.show', $hotel) }}" class="mt-5 inline-block font-semibold hover:text-gold">Explore hotel →</a></div></article>@empty <p class="col-span-full py-16 text-center text-gray-500">No hotels match these filters.</p>@endforelse</div><div class="mt-10">{{ $hotels->links() }}</div></div></section>
@endsection
