@extends('layouts.app')
@section('title', 'Destinations | UAE Tourism')
@section('content')
<section class="bg-deepblack pb-16 pt-36 text-white"><div class="container mx-auto px-6"><p class="text-sm font-semibold uppercase tracking-[.2em] text-gold">Explore the Emirates</p><h1 class="mt-3 text-4xl font-bold md:text-5xl">Destinations</h1><p class="mt-4 max-w-2xl text-gray-300">Discover cities, coastlines, mountains, and cultural landmarks across the UAE.</p></div></section>
<section class="py-16"><div class="container mx-auto px-6"><div class="grid gap-7 sm:grid-cols-2 lg:grid-cols-3">@forelse($destinations as $destination)<a href="{{ route('destinations.show', $destination) }}" class="group overflow-hidden rounded-2xl bg-white shadow-lg"><img class="h-64 w-full object-cover transition duration-500 group-hover:scale-105" src="{{ $destination->getFirstMediaUrl('featured') ?: asset('assets/images/hero.jpg') }}" alt="{{ $destination->name }}"><div class="p-6"><h2 class="text-2xl font-bold">{{ $destination->name }}</h2><p class="mt-1 text-sm text-gold">{{ $destination->city }}, {{ $destination->country }}</p><p class="mt-3 line-clamp-2 text-gray-600">{{ $destination->description }}</p></div></a>@empty <p class="col-span-full py-16 text-center text-gray-500">No destinations are available yet.</p>@endforelse</div><div class="mt-10">{{ $destinations->links() }}</div></div></section>
@endsection
