@extends('layouts.app')
@section('title', $hotel->name.' | UAE Tourism')
@section('content')
<section class="pt-24"><div class="container mx-auto grid gap-10 px-6 py-12 lg:grid-cols-2"><img class="h-[420px] w-full rounded-3xl object-cover" src="{{ $hotel->getFirstMediaUrl('featured') ?: asset('assets/images/hero.jpg') }}" alt="{{ $hotel->name }}"><div><p class="text-gold">{{ str_repeat('★', $hotel->star_rating) }} · {{ $hotel->destination->name }}</p><h1 class="mt-3 text-4xl font-bold">{{ $hotel->name }}</h1><p class="mt-5 text-gray-600">{{ $hotel->description }}</p><p class="mt-4 text-sm text-gray-500">{{ $hotel->address }}</p>@if($hotel->amenities->isNotEmpty())<h2 class="mt-7 font-bold">Amenities</h2><div class="mt-3 flex flex-wrap gap-2">@foreach($hotel->amenities as $amenity)<span class="rounded-full bg-gold/15 px-3 py-1 text-sm">{{ $amenity->name }}</span>@endforeach</div>@endif</div></div></section>
<section class="bg-white py-14"><div class="container mx-auto px-6"><h2 class="text-3xl font-bold">Available rooms</h2><div class="mt-6 grid gap-5 md:grid-cols-2 lg:grid-cols-3">@forelse($hotel->rooms as $room)<article class="rounded-2xl border p-6"><h3 class="text-xl font-bold">{{ $room->name }}</h3><p class="mt-2 text-sm text-gray-500">Up to {{ $room->capacity }} guests</p><p class="mt-4 text-xl font-bold text-gold">${{ number_format((float) $room->price_per_night, 2) }} <span class="text-sm font-normal text-gray-500">/ night</span></p><a href="{{ route('bookings.create', ['type' => 'room', 'id' => $room->id]) }}" class="mt-5 block rounded-lg bg-deepblack py-3 text-center font-bold text-white hover:bg-gold hover:text-deepblack">Book room</a></article>@empty <p class="text-gray-500">No rooms are currently available.</p>@endforelse</div></div></section>
@endsection
