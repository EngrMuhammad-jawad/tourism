@extends('layouts.app')
@section('title', $destination->name.' | UAE Tourism')
@section('content')
<section class="relative min-h-[60vh] pt-24 text-white"><img class="absolute inset-0 h-full w-full object-cover" src="{{ $destination->getFirstMediaUrl('featured') ?: asset('assets/images/hero.jpg') }}" alt="{{ $destination->name }}"><div class="absolute inset-0 bg-black/60"></div><div class="container relative mx-auto flex min-h-[60vh] items-end px-6 pb-16"><div><p class="text-gold">{{ $destination->city }}, {{ $destination->country }}</p><h1 class="mt-2 text-5xl font-bold">{{ $destination->name }}</h1></div></div></section>
<section class="py-16"><div class="container mx-auto grid gap-12 px-6 lg:grid-cols-3"><article class="lg:col-span-2"><h2 class="text-3xl font-bold">About {{ $destination->name }}</h2><div class="prose mt-5 max-w-none text-gray-600">{!! nl2br(e($destination->description)) !!}</div>@if($destination->packages->isNotEmpty())<h2 class="mt-12 text-3xl font-bold">Featured experiences</h2><div class="mt-6 grid gap-5 sm:grid-cols-2">@foreach($destination->packages as $tourPackage)<a class="rounded-xl border bg-white p-5 shadow-sm hover:border-gold" href="{{ route('packages.show', $tourPackage) }}"><h3 class="font-bold">{{ $tourPackage->name }}</h3><p class="mt-2 text-gold">From ${{ number_format($tourPackage->effectivePrice(), 2) }}</p></a>@endforeach</div>@endif</article><aside class="rounded-2xl bg-deepblack p-7 text-white"><h2 class="text-xl font-bold">Plan your visit</h2><p class="mt-3 text-sm text-gray-300">Browse tours and stays curated for this destination.</p><a class="mt-6 block rounded-lg bg-gold px-4 py-3 text-center font-bold text-deepblack" href="{{ route('packages.index', ['destination' => $destination->id]) }}">View packages</a></aside></div></section>
@endsection
