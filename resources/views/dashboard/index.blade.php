@extends('layouts.app')
@section('title', 'My Bookings | UAE Tourism')
@section('content')
<section class="pt-32"><div class="container mx-auto px-6 py-12"><div class="flex items-end justify-between"><div><h1 class="text-4xl font-bold">My bookings</h1><p class="mt-2 text-gray-600">Track and manage your travel requests.</p></div><a class="font-semibold text-gold" href="{{ route('account.edit') }}">Account settings</a></div>@if(session('success'))<div class="mt-6 rounded-lg bg-green-50 p-4 text-green-700">{{ session('success') }}</div>@endif<div class="mt-8 overflow-x-auto rounded-2xl bg-white shadow"><table class="min-w-full text-left text-sm"><thead class="bg-gray-50 text-gray-500"><tr><th class="px-5 py-3">Reference</th><th class="px-5 py-3">Service</th><th class="px-5 py-3">Travel date</th><th class="px-5 py-3">Total</th><th class="px-5 py-3">Status</th><th class="px-5 py-3"></th></tr></thead><tbody class="divide-y">@forelse($bookings as $booking)<tr><td class="px-5 py-4 font-semibold">{{ $booking->booking_number }}</td><td class="px-5 py-4">{{ $booking->bookable?->name ?? 'Unavailable service' }}</td><td class="px-5 py-4">{{ $booking->travel_date->format('d M Y') }}</td><td class="px-5 py-4">${{ number_format((float) $booking->total_price, 2) }}</td><td class="px-5 py-4 capitalize">{{ $booking->status->value }}</td><td class="px-5 py-4">@if($booking->status->isCancellable())<form method="POST" action="{{ route('bookings.cancel', $booking) }}">@csrf<button class="text-red-600 hover:underline">Cancel</button></form>@endif</td></tr>@empty<tr><td colspan="6" class="px-5 py-12 text-center text-gray-500">You do not have any bookings yet.</td></tr>@endforelse</tbody></table></div><div class="mt-8">{{ $bookings->links() }}</div></div></section>
@endsection
